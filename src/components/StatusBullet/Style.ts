import { makeStyles } from '@material-ui/core/styles';

const Style = makeStyles(theme => ({
  bullet: {
    width: 8,
    height: 8,
    borderRadius: 999,
    marginLeft: 16,
  },
}));

export default Style;
